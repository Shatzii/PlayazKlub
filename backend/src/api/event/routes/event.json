{
  "routes": [
    {
      "method": "GET",
      "path": "/events",
      "handler": "event.find",
      "config": {
        "policies": [],
        "auth": false
      }
    },
    {
      "method": "GET",
      "path": "/events/:id",
      "handler": "event.findOne",
      "config": {
        "policies": [],
        "auth": false
      }
    },
    {
      "method": "GET",
      "path": "/events/:id/check-access",
      "handler": "event.checkAccess",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/events/:id/stream-url",
      "handler": "event.getStreamUrl",
      "config": {
        "policies": []
      }
    }
  ]
}